import { CovertToSpacesPipe } from './covert-to-spaces.pipe';

describe('CovertToSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new CovertToSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
